# @nx/angular@23 peer-caps @angular-devkit/core at <22, but this workspace is on Angular 22
# (@angular-devkit/core@22). The combination works in practice; this lets `npm ci` (CI) and
# `npm install` (local) resolve without an ERESOLVE peer conflict, consistently.
legacy-peer-deps=true
